#include <stdio.h>

int main(void)
	{
		int i, j;
		scanf("%d %d", arr[i], j);
		for(int i = 1; arr[i]; i++){
			if (arr[i] == arr[i-1]){
				printf("%d", arr[i] + 1);
			}
			else{
				printf("%d", arr[i]);
			}
			
			








		return 0 ;
	}
